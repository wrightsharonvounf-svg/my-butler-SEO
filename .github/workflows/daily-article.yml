name: 🤖 Ежедневная генерация статьи

# Запуск: 
# - По расписанию (каждый день в 9:00 UTC = 12:00 МСК)
# - Вручную (workflow_dispatch)
# - При push в main, кроме коммитов от батлера
on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:
  push:
    branches: [ main ]
    # Исключаем запуск, если коммит от батлера
    exclude:
      - author_email: 'butler@zaebis.store'

jobs:
    update:
    needs: generate
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write

    steps:
      - name: 🔍 Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          fetch-depth: 0

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 📦 Install dependencies
        run: npm install

      - name: 🔄 Update old post
        run: node update-old-post.js

      - name: 📤 Commit and push
        run: |
          git config user.name "My Butler Factory"
          git config user.email "butler@zaebis.store"
          git add .
          git commit -m "🔁 Авто: обновлена старая статья" || exit 0
          git push
